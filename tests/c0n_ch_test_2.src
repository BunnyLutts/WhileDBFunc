var p;
var q;
var size_of_char;
size_of_char = 8;
var size;
size = 10;
p = malloc(size * size_of_char);
q = p;
while (q - p < size * size_of_char) do {
    var ch;
    ch = read_char();
    *q = ch;
    q = q + size_of_char;
};
var p_;
p_ = p;
while (p_ - p < size * size_of_char) do {
    write_char(*p_);
    write_int(*p_);
    p_ = p_ + size_of_char;
};
var ch_another;
ch_another = read_char();
write_char(ch_another);